<div>
    <label class="label" for="{{ $name }}">
        <span class="text-base label-text">{{ $label }}</span>
    </label>
    <input name="{{ $name }}" id="{{ $name }}" type="text" placeholder="Email Address" class="w-full input input-bordered input-primary" />
</div>
